<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.getfitapp">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/lift"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lift"
        android:supportsRtl="true"
        android:theme="@style/Theme.GetFitApp"
        tools:targetApi="31">
        <activity android:name=".UpLegsActivity"
            android:exported="false"/>

        <activity android:name=".TotBodActivity"
            android:exported="false"/>

        <activity android:name=".CoreActivity"
            android:exported="false"/>

        <activity android:name=".ChestActivity"
            android:exported="false"/>
        <activity
            android:name=".BackActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <queries>
        <intent>
            <action android:name="com.google.android.youtube.api.service.START" />
        </intent>
    </queries>
</manifest>